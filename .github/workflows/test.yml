name: Transform tests

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  transform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: ./test.sh
